{
  "name": "@workspacekit/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "dev": "bun run --watch src/index.ts",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@workspacekit/k8s": "workspace:*",
    "@workspacekit/types": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "^1.1.0",
    "typescript": "^5.7.0"
  }
}
